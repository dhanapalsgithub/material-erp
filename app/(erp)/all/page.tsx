import Link from 'next/link';
const modules=[['Sales billing','Create a GST invoice and deduct stock','/billing'],['Purchase entry','Record incoming goods and increase stock','/purchases'],['Inventory','Items, stock levels and adjustments','/inventory'],['Customers','Customer contacts and GST details','/masters/customers'],['Suppliers','Supplier contacts and GST details','/masters/suppliers'],['Reports','Registers, date filters and Excel download','/reports'],['Company settings','GST profile and staff accounts','/settings']];
export default function AllModules(){return <><h1 className="text-2xl font-bold">All modules</h1><p className="mt-1 text-sm text-slate-500">Quick access to every business function.</p><div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{modules.map(([name,detail,href])=><Link href={href} className="card transition hover:border-teal-600 hover:shadow-md" key={href}><h2 className="font-semibold">{name}</h2><p className="mt-2 text-sm text-slate-500">{detail}</p></Link>)}</div></>}
