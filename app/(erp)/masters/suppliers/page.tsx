import { db } from '@/lib/db'; import { saveParty } from '../actions';
export default async function Suppliers(){const people=await db.supplier.findMany({orderBy:{name:'asc'}});return <><h1 className="text-2xl font-bold">Suppliers</h1><section className="card mt-5"><form action={saveParty.bind(null,'supplier')} className="grid gap-3 md:grid-cols-3"><input name="name" placeholder="Name / company" required/><input name="phone" placeholder="Phone"/><input name="gstin" placeholder="GSTIN"/><input name="address" placeholder="Address"/><input name="state" placeholder="State"/><input name="stateCode" placeholder="State code"/><button className="btn">Add supplier</button></form></section><section className="card mt-5 overflow-x-auto"><table className="table"><thead><tr><th>Name</th><th>Contact</th><th>GSTIN</th><th>State</th></tr></thead><tbody>{people.map(p=><tr key={p.id}><td className="font-medium">{p.name}</td><td>{p.phone}</td><td>{p.gstin}</td><td>{p.state}</td></tr>)}</tbody></table></section></>}
