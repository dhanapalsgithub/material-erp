import { db } from '@/lib/db'; import { saveParty } from '../actions';
export default async function Customers(){const people=await db.customer.findMany({orderBy:{name:'asc'}});return <Page title="Customers" people={people} kind="customer"/>} function Page({title,people,kind}:{title:string;people:any[];kind:'customer'|'supplier'}){return <><h1 className="text-2xl font-bold">{title}</h1><section className="card mt-5"><form action={saveParty.bind(null,kind)} className="grid gap-3 md:grid-cols-3"><input name="name" placeholder="Name / company" required/><input name="phone" placeholder="Phone"/><input name="gstin" placeholder="GSTIN"/><input name="address" placeholder="Address"/><input name="state" placeholder="State"/><input name="stateCode" placeholder="State code"/><button className="btn">Add {kind}</button></form></section><section className="card mt-5 overflow-x-auto"><table className="table"><thead><tr><th>Name</th><th>Contact</th><th>GSTIN</th><th>State</th></tr></thead><tbody>{people.map(p=><tr key={p.id}><td className="font-medium">{p.name}</td><td>{p.phone}</td><td>{p.gstin}</td><td>{p.state}</td></tr>)}</tbody></table></section></>}
