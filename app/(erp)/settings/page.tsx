import { db } from '@/lib/db'; import { saveCompany } from './actions';
export default async function Settings(){const [company,users]=await Promise.all([db.company.findFirst(),db.user.findMany({select:{id:true,name:true,email:true,role:true,active:true}})]);return <><h1 className="text-2xl font-bold">Company & users</h1><section className="card mt-5"><h2 className="mb-3 font-semibold">Company GST profile</h2><form action={saveCompany} className="grid gap-3 md:grid-cols-2"><input name="name" defaultValue={company?.name} placeholder="Legal business name" required/><input name="gstin" defaultValue={company?.gstin||''} placeholder="GSTIN"/><input name="address" defaultValue={company?.address||''} placeholder="Address"/><input name="state" defaultValue={company?.state||''} placeholder="State"/><input name="stateCode" defaultValue={company?.stateCode||''} placeholder="State code"/><input name="invoicePrefix" defaultValue={company?.invoicePrefix||'INV'} placeholder="Invoice prefix"/><button className="btn">Save company settings</button></form><p className="mt-3 text-xs text-slate-500">E-invoice fields (IRN, acknowledgement and e-way bill) are maintained on every invoice and are ready for a GST-provider API adapter.</p></section><section className="card mt-5"><h2 className="mb-3 font-semibold">Staff accounts</h2><table className="table"><thead><tr><th>Name</th><th>Email</th><th>Role</th><th>Status</th></tr></thead><tbody>{users.map(u=><tr key={u.id}><td>{u.name}</td><td>{u.email}</td><td>{u.role}</td><td>{u.active?'Active':'Inactive'}</td></tr>)}</tbody></table></section></>}
