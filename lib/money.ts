export const n = (value: unknown) => Number(value || 0);
export const rupees = (value: unknown) => new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:2}).format(n(value));
export function calculate(lines: {quantity:number;rate:number;discount?:number;gstRate:number}[], taxType: 'INTRA_STATE'|'INTER_STATE', freight=0) { const subtotal=lines.reduce((a,x)=>a+x.quantity*x.rate,0); const discount=lines.reduce((a,x)=>a+(x.discount||0),0); const taxable=subtotal-discount; const tax=lines.reduce((a,x)=>a+((x.quantity*x.rate-(x.discount||0))*x.gstRate/100),0); const cgst=taxType==='INTRA_STATE'?tax/2:0,sgst=taxType==='INTRA_STATE'?tax/2:0,igst=taxType==='INTER_STATE'?tax:0; const raw=taxable+tax+freight, total=Math.round(raw),roundOff=total-raw; return {subtotal,discount,cgst,sgst,igst,total,roundOff}; }
